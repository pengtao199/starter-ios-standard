# Output Contract

When using this skill to generate or plan code, output should always include:

1. Target directories and files
2. Config changes
3. Localization keys
4. Required rules
5. Whether the task belongs in `Presentation`, `Application`, `Domain`, or `Infrastructure`

For starter-based tasks, never answer only with prose. Always make the structure explicit.
